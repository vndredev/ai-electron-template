# Environment Variables Template
# Copy this file to .env and fill in your values

# App Environment
NODE_ENV=development

# Renderer Variables (prefix with VITE_)
# These are exposed to the renderer process
# VITE_API_URL=http://localhost:3000
# VITE_APP_NAME=AI Electron Template

# Main Process Variables (prefix with MAIN_VITE_)
# These are only available in the main process
# MAIN_VITE_API_KEY=your_secret_key_here

# Preload Variables (prefix with PRELOAD_VITE_)
# These are only available in the preload script
# PRELOAD_VITE_CONFIG=some_config
